<%- include('../partials/head') %>
<body>
	<%- include('../partials/header') %>
	<main class="catalog-main">
		<h2>Mira todos los productos que tenemos disponibles</h2>
		<h3 class="sub-title">¡Hey! llevate unas cuantas frutas para asustar el hambre</h3>
		<div class="products-space">
			<div class="product-row">
				<% for (let i = 0; i < frutas.length && i < 4; i++) { %>
				<a href="/products/detail/<%= frutas[i].id %>">
					<article class="control-article">
						<img src="/images/<%= frutas[i].image%>" alt="<%frutas[i].name%>" />
						<p class="product-name"><%= frutas[i].name%></p>
						<p Class="price">
							Desde $<%= frutas[i].wayToSell == 0 ? frutas[i].kilo : frutas[i].unit%>
						</p>
						<button type="submit" class="">Agregar<i class="fas fa-plus-circle"></i></button>
					</article>
				</a>
				<% }%>
			</div>
		</div>
		<h3>Que no se te olviden las verduras, importantes para crecer sano y fuerte</h3>
		<div class="products-space">
			<div class="product-row">
				<% for (let i = 0; i < verduras.length && i < 4; i++) { %>
				<a href="/products/detail/<%= verduras[i].id %>">
					<article class="control-article">
						<img src="/images/<%= verduras[i].image%>" alt="<%verduras[i].name%>" />
						<p class="product-name"><%= verduras[i].name%></p>
						<p Class="price">
						Desde $<%= verduras[i].wayToSell == 0 ? verduras[i].kilo : verduras[i].unidad%>
						</p>
						<button type="submit" class="">Agregar<i class="fas fa-plus-circle"></i></button>
					</article>
				</a>
				<% }%>
			</div>
		</div>

		<h3>Prueba con algunos condimentos que le puedan dar profundidad al sabor de tus comidas</h3>
		<div class="products-space">
			<div class="product-row">
				<% for (let i = 0; i < hortalizas.length && i < 4; i++) { %>
				<a href="/products/detail/<%= hortalizas[i].id %>">
					<article class="control-article">
						<img src="/images/<%= hortalizas[i].image%>" alt="<%hortalizas[i].name%>" />
						<p class="product-name"><%= hortalizas[i].name%></p>
						<p Class="price">
							Desde $<%= hortalizas[i].wayToSell == 0 ? hortalizas[i].kilo : hortalizas[i].unidad%>
						</p>
						<button type="submit" class="">Agregar<i class="fas fa-plus-circle"></i></button>
					</article>
				</a>
				<% }%>
			</div>
		</div>
	</main>
	<%- include('../partials/footer') %>
</body>
